write function for all html pages that are signed in so that the avatar element letter  in the top menu will have a first letter of the username of the sign in user  


write a function in the head write a function in the header or in the app js so write a function in the header or in the app js so that the html pages in public directory will have a



 write a function in the app js so that the html pages in public directory will have a white/ light team instead of dark 



 separate all the sign up and login and separate all  its url / html for the different users update the menu linkssknilsknil